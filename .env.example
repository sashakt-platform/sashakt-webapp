BACKEND_URL=http://localhost:8000/api/v1
SENTRY_AUTH_TOKEN=
PUBLIC_APP_ENV=development #Options: development, staging, production

# Playwright e2e — seeded admin credentials used to look up a published test
# slug via the backend admin API. Required for the test-landing smoke spec.
E2E_USERNAME=
E2E_PASSWORD=
